import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#e67e25",
    },
    secondary: {
      main: "#2ecc71",
    },
    success:{
        main:"#2ec422"
    }
  },
});

export default theme;